# LeafScan AI — Flask Web Application

## Setup
1. Place your trained model at: `models/leafscan_model.keras`
2. `pip install -r requirements.txt`
3. `python app.py`
4. Open `http://localhost:5000`

Classes: Brown Blight (BB), Healthy (GL), Red Rust (RR), Red Spider Mite (RSM)
