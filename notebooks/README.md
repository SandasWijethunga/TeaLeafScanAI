# notebooks/

Place your existing `LeafScanAI_full_pipeline.ipynb` (data preprocessing,
MLflow-tracked training, and evaluation) in this folder — it wasn't
re-attached in this upload batch, so it isn't included in this generated
submission scaffold. The report notebook (`CourseWork2_MLOps_Report.ipynb`)
references it directly and summarizes its results, but the original notebook
with all training cells and outputs should live here alongside it.
